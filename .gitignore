build/
esp_idf_components/

# clang-tidy static analysis (idf.py -B build_clang clang-check)
build_clang/
# public-shape verification builds (idf.py -B build_public, no pack overlay)
build_public/
# bench-run artifacts (test.ps1 writes dated reports here)
test-reports/
# per-machine bench overrides (copy benchboard.local.example.toml)
benchboard.local.toml
warnings.txt
html_report/
# Prerequisites
*.d

# Object files
*.o
*.ko
*.obj
*.elf

# Linker output
*.ilk
*.map
*.exp

# Precompiled Headers
*.gch
*.pch

# Libraries
*.lib
*.a
*.la
*.lo

# Shared objects (inc. Windows DLLs)
*.dll
*.so
*.so.*
*.dylib

# Executables
*.exe
# except the J2534 PassThru driver installer that ships in this repo
!/drivers/**/*.exe
*.out
*.app
*.i*86
*.x86_64
*.hex

# Debug files
*.dSYM/
*.su
*.idb
*.pdb

# Kernel Module Compile Results
*.mod*
*.cmd
!benchboard.cmd
.tmp_versions/
modules.order
Module.symvers
Mkfile.old
dkms.conf

# docs
docs/.nuxt
docs/node_modules

#Vehicle Profiles
.vehicle_profiles/node_modules/

#managed components
managed_components/

# meatpi-components checkout (own git repo, auto-cloned by the root
# CMakeLists — never commit it into this tree)
components/meatpi/
.vscode/

# Clangd Cache
.cache/
# Python
__pycache__/
*.py[cod]
.pytest_cache/

